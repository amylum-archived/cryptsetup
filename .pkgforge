name 'cryptsetup'
org 'amylum'

licenses 'COPYING'

deps(
  lvm2: '2.02.170-2'
)

configure_flags(
  prefix: '/usr',
  sbindir: '/usr/bin'
)

cflags
harden

build do
  run './autogen.sh', 'CFLAGS' => @forge.cflags.join(' ')
  configure
  make
  install
end

test do
  # TODO: add tests
end
