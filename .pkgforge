name 'cryptsetup'
org 'amylum'

licenses 'COPYING'

deps(
  lvm2: '2.02.167-1'
)

configure_flags(
  prefix: '/usr',
  sbindir: '/usr/bin'
)

cflags
harden

build do
  run './autogen.sh', 'CFLAGS' => "-I#{dep(:lvm2)}/usr/include"
  configure
  make
  install
end

test do
  # TODO: add tests
end
